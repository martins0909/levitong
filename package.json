{
  "name": "levitong",
  "version": "1.0.0",
  "main": "index.js",
  "description": "Fullstack blogging platform with client and server",
  "author": "Madueke Martins",
  "license": "ISC",
  "scripts": {
    "dev": "concurrently \"npm run server --prefix server\" \"npm run dev --prefix client\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@google/genai": "^1.8.0",
    "concurrently": "^9.2.0",
    "emailjs-com": "^3.2.0",
    "marked": "^16.0.0"
  }
}
